# Monad Blockchain Configuration
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
MONAD_CONTRACT_ADDRESS=0x...  # Deploy contract first, then add address here
MONAD_PRIVATE_KEY=0x...        # Your private key (KEEP SECRET!)

# Internet Computer Configuration
KINIC_MEMORY_ID=xfug4-5qaaa-aaaak-apgta-cai  # Your deployed memory canister ID

# IC Identity PEM (base64 encoded or direct PEM content)
# Generate with: base64 -w 0 ~/.config/dfx/identity/default/identity.pem
IC_IDENTITY_PEM=-----BEGIN PRIVATE KEY-----
...
-----END PRIVATE KEY-----

# IC Gateway URL (optional, defaults to https://ic0.app)
IC_URL=https://ic0.app

# Kinic Embedding API endpoint (optional)
EMBEDDING_API_ENDPOINT=https://api.kinic.io

# AI/LLM Configuration
ANTHROPIC_API_KEY=sk-ant-...  # Your Anthropic API key (KEEP SECRET!)

# Optional: Override kinic-cli path
# KINIC_CLI_PATH=./kinic-cli/target/release/kinic-cli
